// WebView entry point - This gets bundled by webpack into webview.js
// The actual React app is rendered inline in extension.ts getWebviewHtml()

// This file is kept for reference - the webview HTML/CSS/JS is embedded
// directly in extension.ts to avoid webpack complexity in the webview

console.log('Raul WebView loaded');
